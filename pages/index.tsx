import type { NextPage } from 'next';
import UrlForm from '../components/home/URLForm';

const Home: NextPage = () => {
    return (
        <div>
            <UrlForm />
        </div>
    );
};

export default Home;
